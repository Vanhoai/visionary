{
    "name": "@visionary/domain",
    "version": "0.0.0",
    "private": true,
    "scripts": {
        "lint": "eslint . --max-warnings 0",
        "check-types": "tsc --noEmit"
    },
    "devDependencies": {
        "@visionary/eslint-config": "workspace:*",
        "@visionary/typescript-config": "workspace:*"
    }
}

